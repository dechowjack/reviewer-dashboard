# Archive

This directory holds repo artifacts that are no longer part of the main product presentation.

Current contents:

- `browser-launcher/` - older macOS launcher files for the browser/localhost workflow

The active product is the packaged macOS desktop app documented in the top-level `README.md`.
