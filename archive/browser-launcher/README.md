# Browser Launcher Archive

This folder contains the older macOS launcher workflow that started the web app in Terminal and opened the browser on localhost.

It is kept for reference only. The primary product is now the packaged macOS desktop app at the top level of this repository.
